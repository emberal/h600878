package nerdy.pickup.lines;

public class GetSnap {

  public static void main(String[] args) {
  
    boolean getPizza = false;
    boolean fjernMatch = false;

    //Definer søt jente
    Nerd navn = new Nerd();
    //Definer meg selv
    Nerd martin = new Nerd();

    if (martin.pickupLine == success) {
      getPizza = true;
      System.out.println(navn.getSnap() );
    }
    else {
      martin.setMood("Trist");
      fjernMatch = true;
    }
  }
}
